const operacion_sumar = (a, b) => a + b;
const operacion_restar = (a, b) => a - b;
const operacion_multiplicar = (a, b) => a * b;
const operacion_dividir = (a, b) => a / b;

module.exports = {
  operacion_sumar,
  operacion_restar,
  operacion_multiplicar,
  operacion_dividir,
};
